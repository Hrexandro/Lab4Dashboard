<UserControl x:Class="ChartsWidget.ChartsWidgetView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="250" Width="400">
    <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="5">
        <StackPanel>
            <TextBlock Text="Wykres liczb"
                       FontSize="18"
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>

            <TextBlock Text="Wpisz np. 10 50 30 80"
                       Margin="0,0,0,10"/>

            <StackPanel x:Name="BarsPanel"
                        Orientation="Horizontal"
                        VerticalAlignment="Bottom"
                        Height="150"/>
        </StackPanel>
    </Border>
</UserControl>